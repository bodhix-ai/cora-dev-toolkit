# =============================================================================
# CORA Validation Tools Configuration
# =============================================================================
# This file serves as a template for validator configuration.
# Copy this file to .env in your project's scripts/validation/ directory
# and fill in your actual values.
#
# IMPORTANT: NEVER commit the .env file to version control!
# =============================================================================

# =============================================================================
# Supabase Configuration (for schema-validator)
# =============================================================================

# Supabase REST API Credentials
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Direct PostgreSQL Connection (for accurate schema introspection)
# Get from: Supabase Dashboard → Settings → Database → Connection string
# IMPORTANT: Use DIRECT connection (db.*.supabase.co:5432), NOT pooler
SUPABASE_DB_HOST=db.your-project.supabase.co
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USER=postgres
SUPABASE_DB_PASSWORD=your-database-password

# =============================================================================
# AWS Configuration (for api-tracer and infrastructure validation)
# =============================================================================

# AWS Region
AWS_REGION=us-east-1

# AWS CLI Profile Name (from ~/.aws/credentials)
# Leave empty to use default profile
AWS_PROFILE=your-aws-profile-name

# API Gateway Details (from Terraform output or AWS Console)
# Get API ID from: terraform output -raw modular_api_gateway_id
# Get endpoint from: terraform output -raw modular_api_gateway_url
API_GATEWAY_ID=your-api-gateway-id
API_GATEWAY_ENDPOINT=https://your-api-id.execute-api.us-east-1.amazonaws.com

# =============================================================================
# Notes
# =============================================================================
#
# Schema Validator:
#   - Requires SUPABASE_* variables for database schema validation
#   - Direct PostgreSQL connection is recommended for accuracy
#
# API Tracer:
#   - Requires AWS_* variables for querying API Gateway routes
#   - Can fall back to Terraform parsing if AWS credentials not available
#
# Other Validators:
#   - structure-validator, portability-validator, import-validator, a11y-validator
#   - Do not require .env configuration (use command-line arguments only)
#
# =============================================================================
