name: Deploy App to App Runner

# TODO: Phase 4 - Complete workflow implementation
# This workflow builds Docker image and deploys to AWS App Runner

on:
  push:
    branches: [main]
    paths:
      - 'apps/**'
      - 'packages/**'
      - 'Dockerfile'
      - '.dockerignore'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'dev'
        type: choice
        options: [dev, stg, prd]

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: {{AWS_REGION}}
  ECR_REPOSITORY: {{PROJECT_NAME}}-dev-web

jobs:
  build-and-deploy:
    name: Build and Deploy Web App
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'dev' }}

    steps:
      # TODO: Implement workflow steps
      # 1. Checkout code
      # 2. Configure AWS credentials (use STS central hub-and-spoke OIDC)
      # 3. Login to Amazon ECR
      # 4. Build Docker image
      # 5. Push to ECR (with git sha and latest tags)
      # 6. Deploy to App Runner
      
      - name: Placeholder
        run: |
          echo "TODO: Implement App Runner deployment workflow"
          echo "See: docs/plans/plan_app-runner-monorepo.md Phase 4"